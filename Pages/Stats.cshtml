@page
@model NoodlesSimulator.Pages.StatsModel
@{
    ViewData["Title"] = "📊 סטטיסטיקה אישית";
}

<head>
    <meta charset="utf-8" />
    <title>Noodles Simulator — 📊 סטטיסטיקה אישית</title>
    <link rel="stylesheet" href="/css/site.css" />
    <link rel="icon" type="image/png" sizes="32x32" href="/logo/noodles-logo-transparent.png">
    <link rel="icon" type="image/png" sizes="192x192" href="/logo/noodles-logo-transparent.png">
    <link rel="apple-touch-icon" sizes="192x192" href="/logo/noodles-logo-transparent.png">
    <link rel="manifest" href="/manifest.json">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="theme-color" content="#222222">
</head>
<img src="/assets/background.gif" class="background-gif" alt="" aria-hidden="true" />

<partial name="Shared/_TopBar" />
<partial name="Shared/_LogoHeader" />

<div class="quiz-container stats-page">
    <h2 class="stats-title">📊 סטטיסטיקה אישית</h2>

    <!-- הצגת הנתונים -->
    <ul class="stats-list">
        <li>👤 Logged in as: <strong class="stats-breakable">@Model.Username</strong></li>
        <li>✅ תשובות נכונות: <strong>@Model.CorrectAnswers</strong></li>
        <li>📋 שאלות שנענו: <strong>@Model.TotalAnswered</strong></li>
        <li>📈 אחוז הצלחה: <strong>@Model.SuccessRate</strong>%</li>
    </ul>

    <!-- תצוגת DEBUG -->
    <div class="stats-debug">
        <p><strong>Debug:</strong></p>
        <p>Username: <span class="stats-breakable">@Model.Username</span></p>
        <p>Correct: @Model.CorrectAnswers</p>
        <p>Total: @Model.TotalAnswered</p>
        <p>Success: @Model.SuccessRate%</p>
    </div>

    <div class="stats-actions">
        <a href="/Index" class="next-question-btn">⬅ חזרה לחידון</a>
    </div>
</div>

<partial name="Shared/_SiteFooter" />