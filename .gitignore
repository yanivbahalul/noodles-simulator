bin/
obj/
out/
.DS_Store
.cursor/
.env
data-keys/

wwwroot/reports/*.json
wwwroot/progress/*.json
progress/*.json

# Dev / testing — not needed for Railway deploy
.mcp.json
postman/
scripts/

# Supabase — local CLI + manual SQL editor, not versioned
supabase/

# macOS admin widget — local Xcode project, not deployed
widget/

# Explanation video generation — local only (never push to GitHub)
tools/generate-explanations/

# Question image normalizer — local only
tools/normalize-questions/

# Local explanation demos, TTS experiments, review batches
wwwroot/demo/explanations/
wwwroot/demo/polish-debug-*/
wwwroot/demo/tts-compare/
wwwroot/demo/explanation-demo-*
wwwroot/demo/explanation-frame-*
wwwroot/demo/explanation-demo*.mp4
